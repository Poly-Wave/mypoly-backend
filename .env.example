# .env.example (공통 인프라 환경변수)
# 실제 값은 .env로 복사해서 사용하고, .env는 gitignore 처리하세요.

# PostgreSQL (docker-compose DB 컨테이너용)
POSTGRES_DB=mypoly
POSTGRES_USER=mypoly
POSTGRES_PASSWORD=실제_비밀번호_여기에_입력
POSTGRES_PORT=5432
