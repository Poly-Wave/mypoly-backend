apiVersion: v1
kind: Secret
metadata:
  name: mypoly-jwt-secret
  namespace: mypoly
type: Opaque
stringData:
  # Access Token 서명키 (모든 서비스에서 동일 값 사용)
  JWT_SECRET: "실제_JWT_시크릿_여기에_입력"

  # Refresh Token 서명키 (Access와 반드시 다른 값 사용)
  REFRESH_JWT_SECRET: "실제_REFRESH_JWT_시크릿_여기에_입력"