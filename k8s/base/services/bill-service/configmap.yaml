apiVersion: v1
kind: ConfigMap
metadata:
  name: bill-service-config
  namespace: mypoly
data:
  SPRING_PROFILES_ACTIVE: "local"
  SERVER_PORT: "8080"
  SERVER_FORWARD_HEADERS_STRATEGY: "framework"

  DB_URL: "jdbc:postgresql://mypoly-postgres:5432/mypoly"

  # Bill Category Icons (GCS Public)
  BILL_CATEGORY_ICON_BASE_URL: "https://storage.googleapis.com/mypoly-assets-dev"